
import React from 'react';

const BeautifullScreen = ({answer}) => {
    return (
        <div className="screen">
            <span>{answer}</span>
        </div>
    );
};

export default BeautifullScreen;